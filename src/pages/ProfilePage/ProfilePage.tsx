import { ProfileData } from "widgets/ProfileData/model/ProfileData";

const ProfilePage = () => {
    return (
        <div>
            <ProfileData />
        </div>
    );
};

export default ProfilePage;